apiVersion: 1

datasources:
  - name: 'prtg-datasource'
    type: 'srgssr-prtg-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      serverUrl: 'https://prtg.example.com'
      authMode: 'apiKey'
      tlsSkipVerify: false
    secureJsonData:
      apiKey: 'api-key'
